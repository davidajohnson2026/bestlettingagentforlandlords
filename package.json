{
  "name": "bestlettingagentforlandlords",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
	"scripts": {
		"dev": "astro dev",
		"build": "astro build && node scripts/check-seo.mjs && node --experimental-strip-types scripts/check-postcodes.mjs",
		"preview": "astro preview",
		"astro": "astro",
		"check:seo": "node scripts/check-seo.mjs",
		"check:postcodes": "node --experimental-strip-types scripts/check-postcodes.mjs"
	},
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.4"
  }
}
